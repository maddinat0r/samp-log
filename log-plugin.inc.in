/**
 * Log plugin v@LOG_PLUGIN_VERSION@
 * by maddinat0r
 */

#if defined log_plugin_included
	#endinput
#endif
#define log_plugin_included


enum E_LOG_LOGLEVEL
{
	NONE = 0,
	DEBUG = 1,
	INFO = 2,
	WARNING = 4,
	ERROR = 8,
};


native Logger:CreateLog(const name[], E_LOG_LOGLEVEL:level = INFO | WARNING | ERROR, bool:debuginfo = true);
native DestroyLog(Logger:logger);

native SetLogLevel(Logger:logger, E_LOG_LOGLEVEL:level, bool:enabled);
native bool:IsLogLevel(Logger:logger, E_LOG_LOGLEVEL:level);

native Log(Logger:logger, E_LOG_LOGLEVEL:level, const msg[], {Float,_}:...);
